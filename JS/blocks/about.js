const aboutImage = document.querySelector('.about__image');

if(aboutImage) {
    aboutImage.addEventListener("click", function() {
        alert("Join to the Dark Side. We have cookie's!");
    });
}

